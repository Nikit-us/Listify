spring:
  application:
    name: listify
  datasource:
    url: jdbc:postgresql://localhost:5432/listify_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect